@using System.Diagnostics
@using System.Reflection

@{
    var version = typeof(IdentityServer4.Hosting.IdentityServerMiddleware).Assembly.GetCustomAttribute<AssemblyInformationalVersionAttribute>()?.InformationalVersion.Split('+').First();
}

<div class="welcome-page">
    <h1>
        @*<img src="~/general.brand-logo_v3.svg.svg">*@
        Welcome to Betsson Demo 0Auth2 Server
    </h1>

    <ul>
        <li>
            At the 
            <a href="~/.well-known/openid-configuration">discovery document</a>
             you can find metadata and links to all the endpoints, key material, etc.
        </li>
        <li>
            Click <a href="~/diagnostics">here</a> to see the claims for your current session.
        </li>
        <li>
            Click <a href="~/grants">here</a> to manage your stored grants.
        </li>
    </ul>
</div>
